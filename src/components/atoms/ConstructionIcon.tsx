export function ConstructionIcon({ className }: { className?: string }) {
    return (
        <span className={`i-emojione-monotone-construction bg-gray-800 ${className ? ` ${className}` : ''}`}></span>
    )
}